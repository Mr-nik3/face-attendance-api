[phases.setup]
nixPkgs = ["libGL", "libxcb", "glib", "libSM", "libXext", "libXrender"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip uninstall opencv-python -y || true",
  "pip install opencv-python-headless"
]
